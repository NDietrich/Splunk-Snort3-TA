# we don't want to add these files or folders to the repo

# the build folder is a temporary place we have while building the .spl file
/src/build/

# the SPL file should not be tracked
/src/TA_Snort3_json.spl
/src/TA_Snort3_json.*.spl
# ignore when we're creating new json sample files

/sample\ JSON\ data/alert_json.txt.1*

# ignore md files that have been converted to html files
/README.html
/src/TA_Snort3_json/README.html

# let's not push the gitignore file up either
/.gitignore